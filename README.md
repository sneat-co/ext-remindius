# ext-remindius

Public contract repository for Remindius. `frontend/` is the sole publisher of
`@sneat/extension-remindius-contract`; the paired `remindius` repository owns
the runtime package and standalone application.
